# Tech Stack

> This project is in early setup. Update this file as the stack is established.

## Current State

No source files or build configuration exist yet. The repository contains only a git history and Kiro workspace files.

## Conventions to Follow (until stack is decided)

- Prefer well-established, actively maintained libraries
- Pin dependency versions exactly (no open ranges like `^` or `~`)
- Never commit secrets, API keys, or credentials — use environment variables or a secrets manager
- All financial arithmetic must use decimal-safe libraries (e.g., avoid floating-point for money calculations)

## Common Commands

_To be filled in once the build system is chosen._

```
# Example placeholders — replace with real commands
install:   <package manager install>
build:     <build command>
test:      <test command>
lint:      <lint command>
start:     <dev server command>
```
